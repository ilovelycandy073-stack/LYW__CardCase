<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.BestApplication">
        <activity
            android:name=".feature.idcard.IdCardListActivity"
            android:exported="false" />
        <activity
            android:name=".feature.bankcard.scan.BankCardScanActivity"
            android:exported="false" />
        <activity
            android:name=".feature.bankcard.BankCardDetailActivity"
            android:exported="false" />
        <activity
            android:name=".feature.bankcard.BankCardListActivity"
            android:exported="false" />
        <activity android:name=".feature.settings.SettingsActivity" />
        <activity android:name=".feature.idcard.scan.IdCardScanActivity" />
        <activity android:name=".feature.idcard.IdCardDetailActivity" /> <!-- <activity android:name="com.example.bestapplication.feature.bankcard.BankCardListActivity" /> -->
        <activity
            android:name=".feature.documents.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>